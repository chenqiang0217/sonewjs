class Mesh{
    constructor (scene){
        this.scene = scene
    }
}



// // 节点
// // 新增
// const addPointsInScene = (nos, prefix = CONSTANT.VIEW.PREFIX.MESH.NODE.PREP, step = 0) => {
// }
// // 移动（相连单元移动）
// const movePointsInScene = (nos, prefix = CONSTANT.VIEW.PREFIX.MESH.NODE.PREP, step = 0) => {
// }
// // 复制
// const copyPointsInScene = (nos, prefix = CONSTANT.VIEW.PREFIX.MESH.NODE.PREP, step = 0) => {
// }
// // 删除（相连单元删除）
// const deletePointsInScene = (nos, prefix = CONSTANT.VIEW.PREFIX.MESH.NODE.PREP, step = 0) => {
// }

// 单元
// 新增

// 移动

// 复制

// 删除